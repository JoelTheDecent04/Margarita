#include "Light.h"
#include "Space.h"

/*
Light::Light(std::shared_ptr<Entity> parent)
	: Entity(tLight, parent->fX, parent->fY)
{
	this->parent = parent;
	bCanCollide = false;
	nType = Type::Light;
}

bool Light::Update(float deltatime)
{
	fX = parent->fX;
	fY = parent->fY;
	return true;
}*/